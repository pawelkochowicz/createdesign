"use strict";
console.log(`Hello world!`);

// const galleryPhoto = document.querySelectorAll('.content__gallery--photo');
// const galleryTitle = document.querySelectorAll('.gallery__title');

// galleryPhoto.forEach(title => title.addEventListener('mouseover', (e) => {
//     galleryTitle.forEach(element => element.classList.toggle('gallery__title--visible')
// )}));

// galleryPhoto.forEach(title => title.addEventListener('mouseout', (e) => {
//     galleryTitle.forEach(title => title.classList.toggle('gallery__title--visible')
// )}));

// galleryPhoto.forEach(addEventListener('mouseout', (e) => {
//     galleryTitle.forEach(classList.remove('gallery__title--visible'))
// }));




// galleryPhoto.addEventListener('mouseover', (e) => {
//     galleryTitle.classList.toggle('gallery__title--visible')
// });

// galleryPhoto.addEventListener('mouseout', (e) => {
//     galleryTitle.classList.remove('gallery__title--visible')
// });

